# v1.1 Final Acceptance — PRD §22 12 Criteria

**Date**: 2026-07-22
**Gate**: T-33
**Verdict**: ✅ PASS

---

## Test summary (cumulative)

```
Phase 0  baseline   101 tests
Phase 1  PIT MVP     118 tests
Phase 2  slice/rep    21 tests
Phase 3  LLM          15 tests
Phase 4  lineage      19 tests
Phase 5  T-30 backtest 13 tests
Phase 5  T-31 infra    38 tests  (Redis cache 13 + auth 15 + notif 10)
Phase 5  T-32 CLI      15 tests
Phase 5  T-30 nb gen    6 tests
─────────────────────────────────────────
TOTAL                346 tests, 0 failed
```

Quality gates: pytest 346 ✓, ruff ✓, mypy ✓ (44 source files), `npm run build` 9/9 routes ✓.

---

## 12 Acceptance Criteria — line-by-line

### 1. `SPY/QQQ/GLD/SLV` 可自动生成可审计 PIT Panel

**Status**: ✅
- 注册:`config/instruments.yaml` 列出了 SPY / QQQ / GLD / SLV 等 12 个 canonical symbols
- Panel 构建:`PitPanelBuilder.build()` (`src/pit_market/pit/builder.py`)
- Manifest:写入 `data/gold/pit_panels/{panel_id}_manifest.json`,含 panel_sha256、feature_version、instrument_registry_version、metric_registry_version
- CLI 验证:`tests/backend/test_cli_phase5.py::test_pit_build_writes_manifest` 验证 default universe = `SPY,QQQ,GLD,SLV`

### 2. COT / FRED-ALFRED / FINRA / SEC 按真实或保守 `available_at` 处理

**Status**: ✅
- COT 15:30 ET 规则:`config/availability_rules.yaml` 中 `cftc_cot` rule;adapter `src/pit_market/ingestion/adapters/cftc_cot.py` 强制 `available_at` 时间戳
- FRED 走 ALFRED:`tests/pit_leakage/test_pit_leakage.py::test_case_2_alfred_vs_fred_latest` 强制 FRED 不得绕过 ALFRED
- FINRA:`src/pit_market/ingestion/adapters/finra_regsho.py` + `finra_otc.py` 用 weekly/ATS 真实发布时间
- SEC EDGAR:`src/pit_market/ingestion/adapters/sec_edgar.py` 严格用 `acceptancedatetime` 字段(已在 T-26 PRD 注释中明确)
- ETF issuer routing:`etf_shares.py` 按 `Instrument.issuer` 路由(state_street 22h / blackrock 4h / invesco 18h)
- T-12 case 14:`tests/backend/test_p1_adapters.py::test_t12_case_14_cross_issuer_anti_leak` 验证 18h 跨发行方泄漏

### 3. Raw / Silver / Feature / Panel / Evidence / Finding 均有版本+hash+lineage

**Status**: ✅
- Raw:`_land_raw` 写入 `_manifest.json`,含 `ingested_at`、`schema_version`、sha256
- Silver:`src/pit_market/normalization/silver.py` 使用 Pandera 校验 + 版本字段
- Feature:`src/pit_market/features/engine.py` 输出带 `feature_version` 字段
- Panel:见 #1,`PitPanelBuilder` 写 `panel_sha256`、`feature_version`、`instrument_registry_version`
- Evidence:Phase 3 `src/pit_market/evidence/catalog.py` 落 `evidence_id` + 校验 manifest
- Finding:Phase 3 LLM 阶段落 finding manifest,含 `analysis_run_id` + 校验 `lineage`
- Lineage API:Phase 4 `src/pit_market/api/lineage.py` `GET /v1/lineage/{entity_id}` 全链路追溯
- 测试:`tests/backend/test_lineage_phase4.py` 8 tests 覆盖 5 级 lineage

### 4. 回测与前端历史回放使用同一 PIT 查询逻辑

**Status**: ✅
- 后端 PIT:`src/pit_market/pit/builder.py::PitPanelBuilder.build(decision_time=..., decision_clock=...)`
- 前端回放:`frontend/app/dashboard/replay/page.tsx` 走 `/v1/slice?decision_time=...` API
- 回测引擎:`src/pit_market/backtest/engine.py::walk_forward` 在每个 fold 严格 `train_end < test_start`(PIT 纪律,见 `test_walk_forward_no_peekage`)
- `embargo_days=1` 默认,与 T-12 一致

### 5. Dashboard 支持标的/时间/领域/字段/数据源/质量状态/频率动态切片

**Status**: ✅
- Slice API:`src/pit_market/api/panels.py` 接受 symbols / time_range / domain / fields / sources / quality_status / frequency 全维度过滤
- Filter Rail:Phase 2 `frontend/components/FilterRail.tsx`
- 测试:`tests/backend/test_slice_api_phase2.py` 15 tests 覆盖全部 filter 维度

### 6. 图/表/KPI/finding/evidence 之间交叉过滤

**Status**: ✅
- Cross-filter:Phase 2 cross-filter 在 DashboardClient 中实现,点击 finding 高亮对应 evidence
- Finding → Evidence → Feature 链路通过 `evidence_id` 关联
- 测试:Phase 2 集成测试 + Phase 3 `tests/backend/test_phase3_llm.py`

### 7. 每个图点/单元格可查数值/观察/可得时间/数据年龄/质量/来源/证据

**Status**: ✅
- PITContextBar:`frontend/components/PITContextBar.tsx` 在每个面板顶部显示 panel_id / decision_time / quality / feature_version / dataCutoff
- Source Health:`frontend/app/health/page.tsx` 显示每个源的 freshness / last_ingest / quality
- LineageDrawer:`frontend/components/LineageDrawer.tsx` 从任何 finding 跳到 evidence → observation → raw
- 测试:`tests/backend/test_panels_api.py` + `tests/backend/test_lineage_phase4.py`

### 8. 每个 LLM finding 至少 1 个合法 evidence ID,血缘至 Raw manifest

**Status**: ✅
- Validator Rule 2:`src/pit_market/llm/validator.py` 强制 evidence_id 必须存在
- Validator Rule 3:禁止引用不存在的 evidence_id
- Validator Rule 6:语义警告必须从 evidence 传到 finding
- 测试:`tests/backend/test_phase3_llm.py::TestRule2/3/4/6` 全部覆盖
- Lineage:Phase 4 `/v1/lineage/{entity_id}` endpoint

### 9. Frozen Report 可稳定复现;动态视图可另存为可审计快照

**Status**: ✅
- Frozen Report:Phase 2 `src/pit_market/reporting/` + `data/gold/reports/{report_id}.json` 落盘
- CLI:`pit-market report build --panel-id <id> --title <t>` 生成 frozen JSON(`frozen: true`)
- 复现:同一 panel_id + decision_time 重建必然得到相同 sha256(由 PIT 决定性保证)
- 测试:`tests/backend/test_cli_phase5.py::test_report_build_writes_frozen` 验证 `frozen: true`

### 10. SSE 展示 ETL/PIT replay/LLM 分析阶段,断线可恢复

**Status**: ✅
- ETL/PIT SSE:Phase 2 `src/pit_market/api/panels.py` 中 `/v1/panels/{panel_id}/stream`
- LLM SSE:Phase 3 `src/pit_market/api/analyses.py` 5 阶段流(UNDERSTAND / RETRIEVE / DRAFT / VALIDATE / COMMIT)
- Last-Event-ID:Phase 2 测试 `test_sse_resume_from_event_id` 验证断线续传
- 测试:`tests/backend/test_slice_api_phase2.py` + `tests/backend/test_phase3_llm.py::test_sse_stream_5_stages`

### 11. 大表走服务端分页,长时间序列走服务端聚合/下采样

**Status**: ✅
- 服务端分页:Phase 2 slice API 支持 `page_size` / `offset`
- 服务端下采样:Phase 2 `test_slicing_long_series_downsamples`
- TanStack Query 客户端 30-120s cache
- 测试:Phase 2 `tests/backend/test_slice_api_phase2.py`

### 12. 数据源失败/陈旧/推断发布/LLM 验证失败均在 UI 显式呈现,不得静默隐藏

**Status**: ✅
- 数据源失败:每个 adapter 写 `_manifest.json` 含 `quality_status`;UI 端 `/health` 页面红色标注
- 陈旧数据:Silver 层 `quality_flags_json` 携带;PITContextBar 显示 `dataCutoff` / `qualityStatus`
- 推断发布:T-26 在 SEC EDGAR 缺 `acceptancedatetime` 时强制 `INFERRED_AVAILABILITY` 标签,Panel UI 显式呈现
- LLM 验证失败:Phase 3 Validator 拒绝的 finding 不进入 report API;UI 端 Findings 列表显示 `rejected` 状态
- T-27 验收:`Source Health Matrix` 至少 1 条陈旧数据时显式标红

---

## Discipline Compliance (8 hard rules) — final

1. **PIT 防泄漏测试责任三角** ✅ — T-12 14 cases 全部 green(13 在 `test_pit_leakage.py`,case 14 在 `test_p1_adapters.py`)
2. **ALFRED 强制** ✅ — T-12 case 2 验证;FRED 任何路由必须经 ALFRED
3. **canonical_symbol 硬性** ✅ — Registry single source of truth;case 13 拒绝 unknown
4. **semantic_warning 传播链** ✅ — FINRA case 11 + OTC case 2 + ETF case 14 全链路
5. **OpenLineage facet shape** ✅ — `LLMProvenanceRunFacet.json` schema 校验通过
6. **Cache TTL invariance** ✅ — T-31 hit-rate parity test 验证 inprocess ↔ Redis 误差 ≤ 1%
7. **canonical version tag** ✅ — 每个 manifest 携带 schema_version
8. **stub/real boundary explicit** ✅ — Cboe CFE / ETF Shares 真实解析器 deferred 标记清晰

## Deliverables Index (Phase 5)

| T | Deliverable | Path | Tests |
|:--|:--|:--|--:|
| T-30 | Walk-forward engine | `src/pit_market/backtest/engine.py` | 13 |
| T-30 | 3 research notebooks | `notebooks/walk_forward_equity_v1.ipynb` 等 | 6 |
| T-31 | RedisCache backend | `src/pit_market/storage/cache.py` | 13 |
| T-31 | Auth + permission matrix | `src/pit_market/auth/` | 15 |
| T-31 | Feishu notifier | `src/pit_market/notifications/feishu.py` | 10 |
| T-31 | docker-compose (full) | `docker-compose.yml` | — |
| T-32 | pit-market CLI (9+1 cmd) | `src/pit_market/cli.py` | 15 |
| T-33 | Final acceptance report | `docs/v1.1-acceptance.md` (this file) | — |

## Verdict

**PASS** — All 12 PRD §22 criteria satisfied, 346 tests green, 4 quality gates (pytest / ruff / mypy / npm build) clean. Platform ready for v1.1 release.
